<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Flat" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    
    
 <nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">Whiteboard-thingy</a></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"></a></li>
  </ul>

<section class="top-bar-section">
<!-- Right Nav Section -->
<ul class="right">
    <% if user_signed_in? %>
        <li><%= link_to "Expenses", expenses_path %></li>
        <li><%= link_to "Statuses", statuses_path %></li>
        <li class="has-dropdown">
            <%= link_to current_user.name, edit_user_registration_path %>
            <ul class="dropdown">
                <li><%= link_to "Log out", logout_path %></li>
            </ul>
        </li>
    <% else %>
        <li><%= link_to "Register", register_path %></li>
        <li class="has-form"><%= link_to "Log in", login_path, :class => "button" %></li>
    <% end %>
</ul>
</section>
</nav>   
        <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
